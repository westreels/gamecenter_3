import * as patch from './patch';
export declare type ApplyItem<T> = patch.PatchItem<T> | {
    type: 'remove';
    oldPos: number;
    newPos: number;
    length: number;
};
export declare type Apply<T> = ApplyItem<T>[];
export declare function applyPatch<T>(a: T[], patch: Apply<T>): T[];
